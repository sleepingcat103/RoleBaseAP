SET ANSI_NULLS ON

SET QUOTED_IDENTIFIER ON

CREATE TABLE [dbo].[ROLE](
	[ROLE_ID] [varchar](50) NOT NULL,
	[ROLE_NAME] [nvarchar](50) NOT NULL,
	[EDITABLE] [tinyint] NOT NULL,
	[MEMO] [nvarchar](1500) NULL,
	[CREATE_TIME] [datetimeoffset](7) NOT NULL,
	[LAST_UPDATE_TIME] [datetimeoffset](7) NOT NULL,
PRIMARY KEY CLUSTERED 
(
	[ROLE_ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

ALTER TABLE [dbo].[ROLE] ADD  DEFAULT ((1)) FOR [EDITABLE]

ALTER TABLE [dbo].[ROLE] ADD  DEFAULT (getdate()) FOR [CREATE_TIME]

ALTER TABLE [dbo].[ROLE] ADD  DEFAULT (getdate()) FOR [LAST_UPDATE_TIME]