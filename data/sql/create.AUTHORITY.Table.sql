SET ANSI_NULLS ON

SET QUOTED_IDENTIFIER ON

CREATE TABLE [dbo].[AUTHORITY](
	[ID] [int] IDENTITY(1,1) NOT NULL,
	[USER_ID] [varchar](50) NOT NULL,
	[PROJECT_ID] [varchar](50) NOT NULL,
	[ROLE_ID] [varchar](50) NOT NULL,
	[ACTIVE] [tinyint] NOT NULL,
	[MEMO] [nvarchar](1500) NULL,
	[LAST_UPDATE_TIME] [datetimeoffset](7) NOT NULL,
PRIMARY KEY CLUSTERED 
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

ALTER TABLE [dbo].[AUTHORITY] WITH CHECK ADD FOREIGN KEY([USER_ID])
REFERENCES [dbo].[USER] ([USER_ID])

ALTER TABLE [dbo].[AUTHORITY] WITH CHECK ADD FOREIGN KEY([PROJECT_ID])
REFERENCES [dbo].[PROJECT] ([PROJECT_ID])

ALTER TABLE [dbo].[AUTHORITY] WITH CHECK ADD FOREIGN KEY([ROLE_ID])
REFERENCES [dbo].[ROLE] ([ROLE_ID])

ALTER TABLE [dbo].[AUTHORITY] ADD DEFAULT ((1)) FOR [ACTIVE]

ALTER TABLE [dbo].[AUTHORITY] ADD DEFAULT (getdate()) FOR [LAST_UPDATE_TIME]