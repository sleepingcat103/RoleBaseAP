SET ANSI_NULLS ON

SET QUOTED_IDENTIFIER ON

CREATE TABLE [dbo].[PROJECT](
	[PROJECT_ID] [varchar](50) NOT NULL,
	[PROJECT_NAME] [nvarchar](50) NOT NULL,
	[ACTIVE] [tinyint] NOT NULL,
	[AI_CONFIG] [nvarchar](max) NULL,
	[DB_CONFIG] [nvarchar](max) NULL,
	[MEMO] [nvarchar](1500) NULL,
	[CREATE_TIME] [datetimeoffset](7) NOT NULL,
	[LAST_UPDATE_TIME] [datetimeoffset](7) NOT NULL,
PRIMARY KEY CLUSTERED 
(
	[PROJECT_ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

ALTER TABLE [dbo].[PROJECT] ADD  DEFAULT ((1)) FOR [ACTIVE]

ALTER TABLE [dbo].[PROJECT] ADD  DEFAULT (getdate()) FOR [CREATE_TIME]

ALTER TABLE [dbo].[PROJECT] ADD  DEFAULT (getdate()) FOR [LAST_UPDATE_TIME]
