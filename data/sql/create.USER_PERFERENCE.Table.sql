SET ANSI_NULLS ON

SET QUOTED_IDENTIFIER ON

CREATE TABLE [dbo].[USER_PERFERENCE](
	[ID] [int] IDENTITY(1,1) NOT NULL,
	[USER_ID] [varchar](50) NOT NULL,
	[PERFERENCE_NAME] [varchar](50) NULL,
	[PERFERENCE_VALUE] [nvarchar](max) NOT NULL,
	[LAST_UPDATE_TIME] [datetimeoffset](7) NOT NULL,
PRIMARY KEY CLUSTERED 
(
	[ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

ALTER TABLE [dbo].[USER_PERFERENCE] WITH CHECK ADD FOREIGN KEY([USER_ID])
REFERENCES [dbo].[USER] ([USER_ID])

ALTER TABLE [dbo].[USER_PERFERENCE] ADD  DEFAULT (getdate()) FOR [LAST_UPDATE_TIME]
